# [PRD & Harness] 모바일 10인 실시간 넉아웃 슈터: 커피 스트라이크 (Coffee Strike)

> **목적**: 이 문서는 Google Antigravity(또는 Cursor, Claude Code 등 자율 에이전트)에 입력하여 **"검토(Review) ➔ 구현(Implement) ➔ 검증(Verify) ➔ 완성(Complete)"**의 자율 루프를 단일 실행 사이클로 완수할 수 있도록 설계된 하네스 엔지니어링 기반 제품 요구사항 정의서(PRD)입니다.

---

## Part 1. 100% 무료 인프라 & 기술 스택 아키텍처

Vercel은 서버리스(Stateless) 플랫폼이므로 별도 백엔드 서버 비용 없이 **초당 30~60회의 물리 연산 및 10인 실시간 좌표 동기화**를 구현하기 위해 **WebRTC 기반 Star Topology(Host-Authoritative)** 방식을 채택합니다.

```
       [ Vercel Free Tier (Static Web Hosting) ]
                          │
            PeerJS Free Cloud Signaling (Handshake Only)
                          │
           ┌──────────────┴──────────────┐
           ▼                             ▼
    [호스트 기기 (Room Host)] ◀──WebRTC DataChannel──▶ [게스트 클라이언트 1~9]
    - 게임 물리 연산 독점 (30Hz)                      - 가상 조이스틱 터치 입력 전송 (30Hz)
    - 충돌/넉백/낙하/피격 계산                        - 렌더링 보간(Interpolation)
    - 전체 월드 스냅샷 브로드캐스트                   - 자동 조준/발사 타겟 피드백
```

### 1.1 기술 스택 정의 (비용: 0원)
*   **프론트엔드 프레임워크**: `Vite + Vanilla TypeScript + HTML5 Canvas (2D Context)`
    *   *선택 이유*: 무거운 게임 엔진(Phaser/Pixi) 대비 번들 크기가 수십 KB 수준이며, LLM 코딩 에이전트가 단일/소수 파일로 직관적인 물리 엔진을 버그 없이 작성하기에 최적입니다.
*   **P2P 실시간 통신**: `PeerJS` (WebRTC DataChannel 래퍼)
    *   *신호 서버 (Signaling)*: PeerJS 공용 무료 클라우드 (`0.peerjs.com`, STUN: Google Free STUN `stun:stun.l.google.com:19302`)
    *   *통신 방식*: 방 생성자(호스트)가 서버 역할을 겸하며, 게스트 9명은 호스트에게만 입력값(Input)을 보내고 호스트는 33ms마다 동기화 패킷을 브로드캐스트합니다.
*   **배포 환경**: `Vercel Hobby Plan` (무제한 정적 배포)

---

## Part 2. 게임 메카닉스 & 상세 수치 명세서 (GDD)

### 2.1 코어 룰 및 승패 판정
*   **게임 장르**: 모바일 탑다운 2D 아레나 넉아웃 슈터 (스모 + 슈팅)
*   **핵심 승리/패배 메커니즘**:
    *   HP가 0이 되어 사망하는 것이 아닌, **총알의 물리 충격량(넉백)에 의해 플랫폼 밖(경기장 외곽)으로 추락(Ring-out)하면 즉시 탈락**.
    *   **커피 당첨자(패자)**: 1순위: 가장 먼저 맵 밖으로 떨어진 플레이어 / 타임오버 시: 외곽 밀림 누적 거리 최다자 또는 생존 점수 최하위자.
    *   **우승자**: 최후의 1인 (팀전인 경우 최후 생존 팀).

### 2.2 방 설정 파라미터
| 설정 항목 | 옵션 값 | 기본값 | 비고 |
| :--- | :--- | :--- | :--- |
| **참여 인원** | 2인 ~ 최대 10인 | 10인 | 대기실에서 실시간 인원 표시 및 Ready |
| **게임 모드** | 개인전(FFA) / 팀전 | 개인전 | 팀전: 2팀, 3팀, 4팀 분할 (팀별 고유 색상 및 아군 피격 방지) |
| **경기 시간** | 60초(테스트) / 120초(정식) / 180초 | 120초 | 실시간 타이머 UI 표시 |
| **게임 배속** | 1.0x / 1.5x / 2.0x / 3.0x | 1.0x | 전역 타임스케일 (이동, 탄속, 연사, 아이템 주기 모두 비례 가속) |
| **탄약 모드** | 무제한 / 제한 | 무제한 | 제한 모드 시 잔탄 소진 후 1.2초 재장전 쿨다운 발생 |

### 2.3 무기 시스템 (리스폰/스폰 시 4종 중 무작위 1종 장착)
모든 총기는 **자동 조준(Auto-Aim) & 자동 발사(Auto-Fire)**됩니다. (사거리 내 가장 가까운 적을 탐색)

| 무기 구분 | 탄속 | 연사 쿨타임 | 넉백 충격량 (Impulse) | 탄창 (제한 모드) | 사거리 & 탄도 특성 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **1. 밸런스 피스톨** | 보통 | 0.35초 | 150 | 12발 | 중간 사거리, 직진 1발 |
| **2. 헤비 샷건** | 빠름 | 0.85초 | 320 (펠릿당 80 x 4) | 4발 | 단거리, 부채꼴 4발 방사, 초근접 시 폭발적 밀림 |
| **3. 저격 스나이퍼** | 매우 빠름 | 1.30초 | 450 | 3발 | 초장거리 레이저 궤적, 강력한 직선 1타 넉백 |
| **4. 연사 머신건** | 매우 빠름 | 0.10초 | 45 (누적형 밀림) | 35발 | 중단거리, 빠른 연타로 상대를 연속 밀어냄 |

### 2.4 드롭 아이템 (15초 주기 생성, 지속시간 7초, 최대 2개 유지)
*   🔴 **파워 업 (Power)**: 총기 발사 넉백 위력 2.0배 증가 (맞은 적이 2배로 멀리 날아감)
*   🔵 **스피드 업 (Speed)**: 플레이어 기본 이동속도 +50% 증가
*   🟡 **헤비 쉴드 (Shield)**: 플레이어 무게(질량) 3배 증가 (총알 피격 시 밀림 저항력 70% 감소)

### 2.5 엄폐물 및 10초 서든데스 (Sudden Death)
*   **평시**: 맵 내에 총알과 이동을 모두 차단하는 직사각형 바위 엄폐물 4~6개 배치.
*   **종료 10초 전 (서든데스 진입)**:
    *   화면 전체 경고 사이렌 애니메이션 + "SUDDEN DEATH: 엄폐물 제거!" 메시지 출력.
    *   **모든 엄폐물이 즉시 소멸**하여 개활지로 전환.
    *   남은 10초간 모든 플레이어 **탄약 무제한 강제 발동** ➔ 광란의 난타전 유도.

### 2.6 모바일 UI/UX & 조작
*   **조작계**: 화면 좌측 절반 어디든 터치 시 활성화되는 **가상 플로팅 조이스틱(Virtual Dynamic Joystick)**.
*   **공격**: 조작 불필요. 플레이어 주변 락온 원(Circle) 표시 및 사거리 내 가장 가까운 적을 향해 총구가 자동 정렬되어 자동 격발.

---

## Part 3. Antigravity 자율 실행 하네스 루프 (Harness Loop)

에이전트는 아래 정의된 **4단계 사이클(Review ➔ Implement ➔ Verify ➔ Complete)**을 순차적으로 수행하며, 각 Phase의 Gate 조건을 통과해야 다음 Phase로 진행합니다.

```
┌────────────────────────────────────────────────────────┐
│  Phase 0: Architecture & Environment Bootstrap         │
│   - Vite + TypeScript + Canvas + PeerJS 셋업           │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────┐
│  Phase 1: Local Engine Core (검토 > 구현 > 검증)        │
│   - 터치 조이스틱, 물리/넉백, 자동사격, 낙사 판정     │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────┐
│  Phase 2: Rules & Game Systems (검토 > 구현 > 검증)     │
│   - 무기 4종, 아이템, 엄폐물, 10초 서든데스, 배속      │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────┐
│  Phase 3: Multiplayer Networking (검토 > 구현 > 검증)   │
│   - PeerJS 10인 룸, 호스트 연산, 동기화, 개인/팀전     │
└───────────────────────────┬────────────────────────────┘
                            ▼
┌────────────────────────────────────────────────────────┐
│  Phase 4: Polish & Vercel Deploy Check (최종 완성)     │
│   - 커피 당첨자 UI, 모바일 반응형, 빌드 0에러 검증    │
└────────────────────────────────────────────────────────┘
```

---

### [Phase 0] 프로젝트 부트스트랩 (Environment Bootstrap)

*   **실행 명령**:
    ```bash
    npm create vite@latest coffee-strike -- --template vanilla-ts
    cd coffee-strike
    npm install peerjs
    npm install -D lucide
    ```
*   **디렉터리 구조**:
    ```text
    coffee-strike/
    ├── index.html
    ├── src/
    │   ├── main.ts             # 진입점 및 씬 매니저
    │   ├── engine/
    │   │   ├── Game.ts         # 렌더 및 물리 루프
    │   │   ├── Physics.ts      # 넉백, 충돌, 낙하 연산
    │   │   ├── Joystick.ts     # 가상 터치 조이스틱
    │   │   └── Types.ts        # 공통 타입 및 인터페이스
    │   ├── objects/
    │   │   ├── Player.ts       # 플레이어 엔티티
    │   │   ├── Bullet.ts       # 발사체 엔티티
    │   │   ├── Obstacle.ts     # 엄폐물 엔티티
    │   │   └── Item.ts         # 버프 아이템 엔티티
    │   ├── net/
    │   │   ├── Network.ts      # PeerJS 기반 룸 연결 관리
    │   │   └── Protocol.ts     # 호스트-클라이언트 패킷 정의
    │   └── ui/
    │       ├── LobbyUI.ts      # 룸 생성/참가/옵션 UI
    │       └── HUD.ts          # 인게임 타이머/조작/결과 UI
    ├── package.json
    └── tsconfig.json
    ```

---

### [Phase 1] 로컬 코어 물리 및 조작 (Core Gameplay & Physics)

#### 1.1 검토 (Review)
*   모바일 화면비(가로/세로) 대응: 세로 모드(Portrait) 기준 가상 캔버스 해상도 720x1280 고정 및 CSS letterbox 스케일링 검토.
*   넉백 물리 공식 검토:
    $$ec{v}_{target} = ec{v}_{target} + rac{ec{F}_{knockback} 	imes 	ext{ItemMultiplier}}{M_{player}}$$
*   경기장 바운더리 검토: 반지름 $R = 320$px 원형 아레나. 플레이어 중심 좌표 거리가 $R$을 초과하면 `isFalling = true` 후 크기 축소 애니메이션과 함께 사망.

#### 1.2 구현 (Implement)
*   `src/engine/Joystick.ts`: 터치 다운 시 해당 위치에 베이스 링 생성, 드래그 벡터 `(dx, dy)`를 -1.0 ~ 1.0으로 정규화하여 반환.
*   `src/engine/Physics.ts`: 원-원 충돌(플레이어 간), 원-직사각형 충돌(엄폐물), 탄환 피격 충돌 처리.
*   `src/objects/Player.ts`: 가장 가까운 살아있는 적(Bot 더미 3기 배치) 방향 계산 및 쿨타임 주기 자동 발사 로직.
*   낙사 시 'DEAD' 상태 전환 및 플랫폼 밖으로 떨어지는 페이드아웃 효과.

#### 1.3 검증 (Verify)
*   **테스트 시나리오**:
    1. 브라우저 F12 모바일 에뮬레이터 켜기.
    2. 터치 드래그로 캐릭터 부드럽게 8방향 이동 확인.
    3. 더미 봇 근처에 가면 총알이 자동 발사되는지 확인.
    4. 총알에 맞은 봇이 넉백으로 밀려나 맵 밖으로 떨어져 사망하는지 확인.
*   **Gate 조건 (DoD)**: 조이스틱 랙 없음, 60 FPS 유지, 넉백 및 낙사 판정 정상 작동.

---

### [Phase 2] 무기 4종, 아이템, 서든데스, 배속 (Rule System)

#### 2.1 검토 (Review)
*   배속 구현 시 `dt(delta time)`에 배속 배율(`timeScale`: 1.0 ~ 3.0)을 곱해 모든 물리 연산과 타이머를 통제하도록 설계.
*   서든데스 10초 전 이벤트 디스패치 구조 검토: 타이머 감시 중 `timeRemaining <= 10` 도달 시 엄폐물 배열 클리어 및 사운드/화면 이펙트 트리거.

#### 2.2 구현 (Implement)
*   `src/objects/Bullet.ts`: 무기 4종(피스톨, 샷건, 스나이퍼, 머신건) 타입별 탄환 속도, 크기, 궤적 색상, 넉백 수치 분기.
*   `src/objects/Obstacle.ts`: 4개의 직사각형 엄폐물 배치. 총알이 닿으면 총알 소멸, 플레이어가 부딪히면 미끄러짐(Sliding) 충돌 처리.
*   `src/objects/Item.ts`: 맵에 파워/스피드/쉴드 구체 스폰. 플레이어 접촉 시 7초간 버프 플래그 활성화.
*   `src/engine/Game.ts`:
    *   배속 셀렉터(1x, 1.5x, 2x, 3x) 연동.
    *   10초 전 서든데스: 엄폐물 배열 즉시 삭제 (`obstacles = []`), 잔여 탄약 무한대 플래그 셋.

#### 2.3 검증 (Verify)
*   **테스트 시나리오**:
    1. 무기 4종이 정상적으로 다른 탄속과 넉백(샷건은 4발 분산, 스나이퍼는 단발 강타 등)을 보이는지 테스트.
    2. 아이템 획득 시 플레이어 주위에 버프 오라(빨강/파랑/노랑)가 표시되고 효과가 적용되는지 확인.
    3. 60초 타이머 설정 후 10초가 남았을 때 엄폐물이 깨끗이 사라지는지 확인.
    4. 3배속 선택 시 게임 전체 속도가 3배 빨라지는지 확인.
*   **Gate 조건 (DoD)**: 4가지 무기 및 3종 버프 정상 작동, 서든데스 이벤트 정확한 타이밍 발동 확인.

---

### [Phase 3] PeerJS 10인 실시간 네트워크 동기화 (Multiplayer Engine)

#### 3.1 검토 (Review)
*   **네트워크 토폴로지**: Star Topology. 방 생성자 = Host = Room ID (`coffee-xxxxxx`).
*   **패킷 구조**:
    *   `Client -> Host (C2S_Input)`: `{ id, joyX, joyY }` (30Hz)
    *   `Host -> Clients (S2C_State)`: `{ time, players: [{id, x, y, angle, hp, weapon, isDead, team}], bullets: [...], items: [...], suddenDeath: bool }` (30Hz)
*   **팀전 로직**:
    *   팀 선택 모드(2팀: 청/홍, 3팀: 청/홍/녹, 4팀: 청/홍/녹/황).
    *   팀전 시 같은 팀원은 자동 타겟팅에서 제외, 총알 충돌 무시.

#### 3.2 구현 (Implement)
*   `src/net/Network.ts`:
    *   PeerJS 인스턴스 초기화.
    *   `createRoom(options)`: 무작위 6자리 룸 코드 생성 및 Host 루프 활성화.
    *   `joinRoom(roomCode, nickname, team)`: 해당 ID의 Host에 DataChannel 연결.
    *   최대 10명까지 커넥션 수용 및 연결 해제(Disconnect) 처리.
*   `src/ui/LobbyUI.ts`:
    *   방 생성 / 방 참가(코드 입력 또는 공유 링크 URL 쿼리스트링 `?room=xxxxxx`) 지원.
    *   팀 선택 및 참가자 명단 실시간 대기실 동기화.

#### 3.3 검증 (Verify)
*   **테스트 시나리오**:
    1. 한 브라우저 창에서 방 생성(Host).
    2. 새 시크릿 창 2~3개를 띄워 생성된 방 코드로 접속(Clients).
    3. Host와 Client들 간에 조이스틱 입력 시 각 화면에서 상대방 캐릭터가 부드럽게 움직이는지 확인.
    4. 서로를 쏴서 맵 밖으로 밀어냈을 때 모든 화면에서 동시에 사망 판정이 나오는지 확인.
*   **Gate 조건 (DoD)**: 최소 3개 이상의 탭(모바일 뷰)에서 동기화 지연 100ms 미만으로 매끄러운 넉백 판정 및 사망 동기화 성공.

---

### [Phase 4] 커피 당첨자 UI 및 Vercel 빌드 최적화 (Polish & Deploy)

#### 4.1 검토 (Review)
*   결과 화면의 가독성: 1위 승자보다 **"오늘 커피 살 사람(1등 탈락자)"**을 가장 크고 코믹하게 부각.
*   Vercel 빌드 설정: `vite build` 시 Typescript 타입 에러 0건 유지.

#### 4.2 구현 (Implement)
*   `src/ui/HUD.ts`:
    *   상단: 타이머, 남은 생존 인원 카운터, 현재 배속 표시.
    *   종료 모달(Game Over):
        *   ☕ **"오늘 커피 당첨자: [닉네임]!"** (가장 먼저 탈락한 사람 또는 타임오버 시 최하위자)
        *   생존 순위 표 (1등부터 10등까지의 랭킹 및 버틴 시간).
        *   [다시 한 판 하기(Replay)] 버튼 (방장이 누르면 동일 멤버로 재시작).
*   모바일 전체화면 (PWA Fullscreen 스타일 터치 방지 메타태그 적용).

#### 4.3 검증 (Verify & Deploy)
*   **검증 체크리스트**:
    1. `npm run build` 실행하여 `dist/` 빌드 에러가 없는지 확인.
    2. 브라우저 콘솔 에러 0건 확인.
    3. `vercel` CLI 또는 GitHub 연동을 통해 배포 URL 테스트.
*   **Gate 조건 (DoD)**: 모바일 사파리/크롬 브라우저에서 URL 접속만으로 별도 로그인 없이 10초 만에 방 접속 및 10인 게임 구동 완료.

---

## Part 4. Antigravity 에이전트 프롬프트 명령어 (복사용)

구글 안티그래비티 대화창에 아래 명령어를 그대로 입력하여 자율 구현 루프를 가동하십시오:

```text
안녕하세요! 위 PRD 및 Harness 문서(coffee_strike_prd_harness.md)의 모든 요구사항을 완벽히 이해했습니다.
이제 Phase 0부터 Phase 4까지 정의된 [검토 ➔ 구현 ➔ 검증 ➔ 완료] 루프를 자율적으로 순차 실행해주세요.

진행 규칙:
1. 별도의 백엔드 없이 Vercel에 정적 배포할 수 있도록 Vite + Vanilla TypeScript + HTML5 Canvas + PeerJS 스택으로 구현하세요.
2. 각 Phase가 완료될 때마다 Definition of Done(DoD) 셀프 검증 결과를 출력하고 다음 Phase로 진행하세요.
3. 넉백 물리 공식, 4가지 무기 특성, 10초 서든데스 엄폐물 삭제, 10인 WebRTC 동기화, 커피 쏘는 사람 결과 화면이 모두 포함되어야 합니다.
4. 시작해주세요: Phase 0 및 Phase 1 구현을 시작하세요.
```